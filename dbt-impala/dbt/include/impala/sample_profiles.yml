
default:
  outputs:
    dev:
      type: impala
      # Add sample credentials here, like:
      # host: <host>
      # port: <port_num>
      # username: <user>
      # password: <pass>
  target: dev
